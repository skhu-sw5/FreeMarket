import { createStore } from 'vuex'
import auth from './modules/auth'

export default createStore({
  state: {
    // 루트 상태 (필요한 경우)
  },
  mutations: {
    // 루트 뮤테이션
  },
  actions: {
    // 루트 액션
  },
  modules: {
    auth
  }
})
